import { FiltreCourrierPipe } from './filtre-courrier.pipe';

describe('FiltreCourrierPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreCourrierPipe();
    expect(pipe).toBeTruthy();
  });
});
